<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.music.capstone">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ListedMusicsActivity"></activity>
        <activity android:name=".authentication.SignInActivity">

        </activity>
        <activity android:name=".IndexActivity">

        </activity>
        <activity android:name=".authentication.SignUpActivity" />
        <activity android:name=".authentication.ResetPasswordActivity" />
        <activity android:name=".authentication.ForgetPasswordActivity" />
        <activity android:name=".authentication.ChangePasswordActivity" />
        <activity android:name=".Nowplaying"></activity>
        <activity android:name=".authentication.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".profile.EditProfile"></activity>
        <activity android:name=".PlaylistCreateActivity"></activity>
        <activity android:name=".UserPlaylistsActivity"></activity>
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".ViewAllActivity">

        </activity>

    </application>

</manifest>